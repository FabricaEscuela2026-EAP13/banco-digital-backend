apiVersion: v1
kind: Service
metadata:
  name: bancodigital-service
  labels:
    app: bancodigital
spec:
  selector:
    app: bancodigital
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
